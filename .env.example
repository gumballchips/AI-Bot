# Copy to .env and set your API key
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY

# Optional: choose a stronger model if available to your account.
# Example values you might set (depends on your provider/account):
# OPENAI_MODEL=gpt-4o
# OPENAI_MODEL=gpt-4
# OPENAI_MODEL=gpt-4o-mini
OPENAI_MODEL=gpt-4

# App port
PORT=3000
