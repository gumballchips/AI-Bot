# Copy this to a file named ".env" at the project root and fill in your real key.
# Do NOT commit the actual .env file to source control.

OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY
# Optional: choose the model you want the server to use
OPENAI_MODEL=gpt-4
# Optional: change server port
PORT=3000
